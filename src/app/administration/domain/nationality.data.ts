import { Time } from '@angular/common';

export interface NationalityData{
    name: string;
    description: string;
    code: string;
    nationality_id:number;
    created_by: number;
    created_date: Date;
    
}